// set the target number
let num = 20;

// try looping through the array for the total size
for (let index = 2; index <= num; index++) {
  // check for only numbers which are even using the index number
  if (index % 2 === 0) {
    // log / print the result
    console.log(index);
  }
}
